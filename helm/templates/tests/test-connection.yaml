apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digitalthinkers.fullname" . }}-test-connection"
  labels:
    {{- include "digitalthinkers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "digitalthinkers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
